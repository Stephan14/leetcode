#include<iostream>
#include<vector>

class Solution {
public:
    int maxSubArray(vector<int>& nums) {
      int i, j, max = -100000;

      for( i = 0; i < nums.size(); i++ )
        for( j = i; j < mums.size(); j++ )
          max = max < nums[j] ?

    }

    int findmax( int a, int b, int c )
    {
      
    }
};

int main(int argc, char const *argv[]) {
  /* code */
  return 0;
}
